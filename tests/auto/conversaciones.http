
### THIS IS AN AUTO-GENERATED TEST SUITE, IT NEEDS TO BE COMPLETED WITH VALID DATA
### THESE ARE NOT ALL YOU NEED, MORE OF THEM MUST BE CREATED TO EVALUATE THE FUNCTIONAL
### REQUIREMENTS OF THE PROJECT AT HAND, THESE TEST ONLY TEST THE CRUD PORTION OF THE ENTITY.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing conversaciones
### This query is used in several of the below tests it should be executed first.
# @name conversaciones
GET {{BASE}}/conversaciones

### Login a(n) usuarios and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"PASSWORD": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing conversaciones by its id.
### positive test 
@conId = {{conversaciones.response.body.0.conversacionId}}
GET {{BASE}}/conversaciones/{{conId}} 
Content-Type: application/json 


### Test 02: Try get one existing conversaciones by its nonexistent id.
### negative test 
GET {{BASE}}/conversaciones/999999999
Content-Type: application/json 


### Test 03: Add a new conversaciones successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newconversaciones
POST {{BASE}}/conversaciones 
Content-Type: application/json 
Token: {{token}} 

{
	"vinculoId": ###REPLACE###,
	"fechaInicio": ###REPLACE###,
	"fechaFin": ###REPLACE###
} 

### Check the created conversaciones

@newconversacionesid = {{newconversaciones.response.body.lastId}}
GET {{BASE}}/conversaciones/{{newconversacionesid}}
Content-Type: application/json 

### Test 04: Add a new conversaciones without a session token
### Negative test 
POST {{BASE}}/conversaciones 
Content-Type: application/json 

{ 
	"vinculoId": ###REPLACE###,
	"fechaInicio": ###REPLACE###,
	"fechaFin": ###REPLACE###
}

### Test 05: Modify an existing conversaciones 
### Positive test 
@conId = {{conversaciones.response.body.0.conversacionId}} 
PUT {{BASE}}/conversaciones/{{conId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"vinculoId": ###REPLACE###,
	"fechaInicio": ###REPLACE###,
	"fechaFin": ###REPLACE###
}

### Check the modified conversaciones

GET {{BASE}}/conversaciones/{{newconversacionesid}}
Content-Type: application/json 

### Test 06: Try to modify an existing conversaciones without a session token
### Negative test
@conId = {{conversaciones.response.body.0.conversacionId}}
PUT {{BASE}}/conversaciones/{{conId}}
Content-Type: application/json

{
	"vinculoId": ###REPLACE###,
	"fechaInicio": ###REPLACE###,
	"fechaFin": ###REPLACE###
}

### Test 07: Delete an existing conversaciones
### Positive test

### Create a new conversaciones, which we will delete
# @name createdconToDelete
POST {{BASE}}/conversaciones
Content-Type: application/json
Token: {{token}}

{
	"vinculoId": ###REPLACE###,
	"fechaInicio": ###REPLACE###,
	"fechaFin": ###REPLACE###
}

### Check the created conversaciones
@condelId = {{createdconToDelete.response.body.lastId}}
GET {{BASE}}/conversaciones/{{condelId}}
Content-Type: application/json 

### Delete the conversaciones
DELETE {{BASE}}/conversaciones/{{condelId}}
Token: {{token}}

### Check the deleted conversaciones
GET {{BASE}}/conversaciones/{{condelId}}
Content-Type: application/json 

### Test 08: Try to delete a conversaciones without a session token
### Negative test
DELETE {{BASE}}/conversaciones/{{conId}}

